<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhj.lostandfound">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LostAndFound">
        <activity android:name=".AddTalkActivity"></activity>
        <activity android:name=".ShowThingsActivity" />
        <activity android:name=".NewThingActivity" />
        <activity android:name=".loginandregister.ModifyActivity" />
        <activity android:name=".centers.CenterActivity" />
        <activity android:name=".loginandregister.RegisterActivity" />
        <activity android:name=".loginandregister.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>